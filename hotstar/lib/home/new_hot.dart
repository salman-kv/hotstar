import 'package:flutter/material.dart';

class NewHot extends StatelessWidget {
  const NewHot({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('New and Hot',style: TextStyle(color: Colors.white),),
    );
  }
}